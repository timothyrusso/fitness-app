import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    position: 'absolute',
    borderTopWidth: 0,
    marginHorizontal: 20,
    marginVertical: 20,
    borderRadius: 100,
    overflow: 'hidden',
    height: 60,
  },
});

export default styles;
